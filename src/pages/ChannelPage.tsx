import type { Notify } from '../types/domain'
export function ChannelPage({ notify }: { notify: Notify }) { const rows = [['47','哈基米biu-轮询','已启用'],['55','walkcoding-随机','已启用'],['68','沃沃-轮询','已禁用'],['41','mouubox-轮询','已禁用'],['45','小白-轮询','已禁用'],['46','2chat-轮询','已禁用'],['52','grok-chat','已启用'],['54','relaycat-随机','已禁用'],['56','AAA王工-随机','已禁用'],['51','walk-视频','已启用']] as const; return <div className="channel-page"><div className="channel-heading"><div><span className="channel-kicker">管理中心</span><h1>渠道 <small>最多重试: 10 次</small></h1></div><div className="channel-actions"><button onClick={() => notify('批量操作已打开')}>☷ 批量操作</button><button onClick={() => notify('排序已切换')}>↕ 使用 ID 排序</button><button className="channel-primary" onClick={() => notify('创建渠道表单已打开')}>＋ 创建渠道</button></div></div><div className="channel-filters"><input placeholder="按名称、ID 或密钥筛选…"/><input placeholder="按模型筛选…"/><button onClick={() => notify('状态筛选已打开')}>⊕ 状态</button><button onClick={() => notify('分组筛选已打开')}>⊕ 分组</button></div><div className="channel-table"><div className="channel-table-head"><span>ID</span><span>名称</span><span>类型</span><span>状态</span><span>分组</span><span>优先级</span><span>权重</span><span>已使用 / 剩余</span><span>响应</span><span>上次测试</span><span>操作</span></div>{rows.map(row => <button className="channel-row" key={row[0]} onClick={() => notify(`已打开渠道「${row[1]}」`)}><span>{row[0]}</span><span className="channel-name"><strong>{row[1]}</strong></span><span className="channel-type">◉ OpenAI</span><span className={row[2] === '已启用' ? 'channel-enabled' : 'channel-disabled'}>{row[2]}</span><span className="channel-group">默认分组</span><span className="channel-pill">7</span><span className="channel-pill">1</span><span>$0 <small>$0</small></span><span className="channel-latency">2.21 秒</span><span>26 分钟前</span><span className="channel-tools">✎ ◔ ···</span></button>)}</div></div> }
