import { useState } from 'react'
import type { Notify } from '../types/domain'
import { PageHeader } from '../components/common/PageHeader'
const names = ['晨雾里的小屋','胶片人像','山间少年','猫咪日记','夏日白衬衫','湖边散步','窗边肖像','暖色房间','城市夜行','静物练习','海边风声','蓝色花园','纸上概念','午后阳光','玻璃温室']
const gradients = ['gallery-a','gallery-b','gallery-c','gallery-d','gallery-e','gallery-f','gallery-g','gallery-h','gallery-i','gallery-j','gallery-k','gallery-l','gallery-m','gallery-n','gallery-o']
export function AssetsPage({ notify }: { notify: Notify }) { const [filter, setFilter] = useState('全部'); return <div className="library-page"><PageHeader eyebrow="本地资产" title="图库" actions={<button className="light-button" onClick={() => notify('图库已刷新')}>↻ 刷新</button>} /><div className="library-stats"><div className="search-box">⌕<input placeholder="搜索 ID、类型或哈希" /></div><div className="stats"><span>▧ 图片总数 <strong>78</strong></span><span>▤ 占用空间 <strong>714 MB</strong></span></div></div><div className="filter-row">{['全部','最近生成','收藏'].map(item => <button key={item} className={filter === item ? 'selected' : ''} onClick={() => { setFilter(item); notify(`已切换${item}`) }}>{item}</button>)}</div><div className="gallery-grid">{names.map((name,index) => <button className="gallery-card" key={name} onClick={() => notify(`已打开「${name}」`)}><div className={`gallery-image ${gradients[index]}`}><span>{index % 4 === 0 ? '✦' : ''}</span></div><div className="gallery-meta"><strong>{name}</strong><span>img_{String(index+1).padStart(2,'0')}N7P…</span><small>JPEG · {140 + index*17} KB</small></div></button>)}</div></div> }
