import type { Notify, Theme } from '../types/domain'
export function SettingsPage({ theme, setTheme, notify }: { theme: Theme; setTheme: (theme: Theme) => void; notify: Notify }) { return <div className="library-page"><div className="page-heading"><div><span className="eyebrow">偏好设置</span><h1>设置</h1></div></div><div className="settings-card"><h3>外观</h3><p>选择界面主题</p><div className="theme-buttons">{(['system','light','dark'] as const).map(item => <button key={item} className={theme === item ? 'selected' : ''} onClick={() => { setTheme(item); notify(`已切换${item === 'system' ? '系统' : item === 'light' ? '浅色' : '深色'}主题`) }}>{item === 'system' ? '跟随系统' : item === 'light' ? '浅色' : '深色'}</button>)}</div></div></div> }
