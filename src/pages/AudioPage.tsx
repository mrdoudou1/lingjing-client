import { useState } from 'react'
import type { Notify } from '../types/domain'
export function AudioPage({ notify }: { notify: Notify }) { const [tab, setTab] = useState<'tts'|'stt'>('tts'); const [text, setText] = useState(''); return <div className="creative-page voice-page"><div className="creative-header"><div><span className="eyebrow">声音工作室</span><h1>{tab === 'tts' ? '语音合成' : '语音识别'}</h1></div><div className="creative-tabs"><button className={tab === 'tts' ? 'selected' : ''} onClick={() => setTab('tts')}>语音合成</button><button className={tab === 'stt' ? 'selected' : ''} onClick={() => setTab('stt')}>语音识别</button></div></div><div className="voice-card"><textarea className="unified-input" rows={8} value={text} onChange={e => setText(e.target.value)} placeholder={tab === 'tts' ? '输入需要合成的文本…' : '拖入音频或视频文件，生成文本…'}/><div className="voice-fields"><select><option>{tab === 'tts' ? 'Aria · 温暖女声' : '中文（普通话）'}</option><option>River · 平静男声</option></select><select><option>{tab === 'tts' ? 'MP3' : 'SRT'}</option><option>WAV</option></select><button onClick={() => notify(text.trim() ? '试听已准备' : '请先输入内容')}>▶ 试听</button><button className="dark-action" onClick={() => notify(text.trim() ? (tab === 'tts' ? '语音任务已创建' : '识别任务已创建') : '请先输入内容')}>{tab === 'tts' ? '合成语音' : '开始识别'} →</button></div></div></div> }
